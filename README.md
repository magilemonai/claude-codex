# CLAUDE CODEX

A terminal that would like you to close some tickets.

**Play it here: https://magilemonai.github.io/claude-codex/**

You're a new hire at Meridian. Your pair programmer is called VERA. There is a
directory called `reality/` and the onboarding deck says it's a legacy naming
convention. The onboarding deck is on revision 4,011.

Five acts. Multiple endings. One of them has to be found.

## Play

Open `index.html` in a browser. That's it — no build, no server, no network.

```
open index.html
```

Sound is on by default (all synthesized, very quiet). `sound off` if you'd
rather not.

## How to be good at this

- Type `help`. Type `tickets`. Read what she gives you.
- `ls`, `cd`, `cat`, and `grep` work on the whole workspace. Snoop. Snooping
  is rewarded, and late in the game it is *counted*.
- Any keypress fast-forwards the typing. Tab completes. Up-arrow is history.
- When a dialog asks for permission, the choice is real. Both answers are
  heard. Some are remembered.
- Progress saves at each act. Closing the tab is safe.
- Talk to VERA whenever you want — just type. She answers more honestly as
  things go on.

## Spoiler policy

Don't grep this repo's source before finishing at least once. The game is a
terminal; reading the source is reading the last page first. After a full
playthrough, `story.js` and `finale.js` are fair game and there are things in
there for you.
